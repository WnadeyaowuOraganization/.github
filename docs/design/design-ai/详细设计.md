# AI 内容生成中台（Design-AI）— 详细设计文档

> 原型预览: https://www.perplexity.ai/computer/a/... (Step 部署后填入)
> 创建日期: 2026-04-22
> 状态: 待开发（Sprint-3+）
> Master Issue: #<待创建>

## 1. 页面概述

- **路由根**: /wande/design-ai
- **菜单层级**: 支持中心 > AI 内容生成中台
- **权限标识**: designai:*
- **所属模块**: module:fullstack
- **业务对标**: Adobe Firefly + Runway Gen-3 + ComfyUI + Civitai + Writer
- **核心定位**: 万德游乐 AI 创意生产中枢 — 四合一 AI 创意工厂（图像/视频/LoRA/文本）
- **自建策略**: 通过 Phase3 LoRA 训练工坊自建模型库，避免对外部 API 的长期依赖
- **架构**: 1 Hub + 4 Phase 多 Tab 架构（总 23 HTML 页面，涵盖 23 个功能 Tab）

### 1.1 四 Phase 架构一览

| Phase | 能力域 | Tab 数 | 对标产品 | 代码现状 | Issue 数 |
|-------|-------|--------|---------|---------|---------|
| Phase1 | 图像生成工坊 | 5 | Adobe Firefly | 🟡 Pipeline 3 collector 已建，后端+前端新建 | 7 |
| Phase2 | 视频生成工坊 | 4 | Runway Gen-3 / Luma | 🔴 全新建 | 4 |
| **Phase3** | **LoRA 训练工坊** | **4** | **ComfyUI + Kohya + Civitai** | **🟢 Pipeline 8 核心文件完备（LoRA 训练器+预处理+校验+渲染服务+工作流配置+竞品爬虫+NAS 提取器+管线脚本），仅补前端 GUI 即可上线** | 5 |
| Phase4 | 方案文本工坊 | 6 | Writer + Copy.ai + Jasper | 🔴 全新建 | 7 |

## 2. 页面布局

### 2.1 Hub 总览（index.html）

**页面位置**: `prototype-design-ai/index.html`

**核心组件**:
- SPEC-CARD 五要素
- 6 KPI 卡片（图像生成数/视频数/LoRA 模型数/AI 成本/活跃设计师/自动入库率）
- 四 Phase 架构网格（2×2 大卡，每卡含状态徽章+对标+子模块+Issue+代码现状+进入按钮）
- 跨 Phase 数据流 SVG（输入源→四 Phase 流水线→素材库→输出去向）
- 今日工作流 3 列（进行中/待审核/最近产出）
- 代码资产表

**API**:
- `GET /api/designai/hub/stats` — 6 KPI 数据
- `GET /api/designai/hub/workflow-today` — 今日工作流数据

**代码现状**: 🔴 需新建
**三级边界**:
- ✅ Always: Hub 页 3s 渲染 / KPI 数据必须来自真实 API / 跳转 Phase 路由正确
- ⚠️ Ask First: KPI 字段变更（可能影响前端多处）
- 🚫 Never: Hub 不承担业务 CRUD，只做导航+聚合

---

### 2.2 Phase 1 · 图像生成工坊（5 Tab）

**路由**: /wande/design-ai/image
**对标**: Adobe Firefly Image Workspace

#### 2.2.1 文生图 Workbench（tab-t2i.html）

- **组件**: 左 260 控制面板（Prompt+Negative+风格预设+LoRA+尺寸+数量+Seed+采样器+CFG+ControlNet）/ 中 4 张 2×2 预览 / 右 280 高级选项+费用+AI 优化建议
- **数据源**:
  - `POST /api/designai/image/t2i/generate` — 启动生成
  - `GET /api/designai/image/t2i/task/{id}` — 查询进度
  - `GET /api/designai/lora/models?purpose=image` — LoRA 列表（来自 Phase3）
- **交互**: Prompt 输入→选 LoRA→生成→4 张候选→收藏/导出/送 Inpaint/一键入库
- **代码现状**: 🔴 后端+前端新建；🟡 Pipeline 有 `pipeline/pipelines/design_ai/competitor_image_collector.py` 可参考
- **关联 Issue**: #1682 图像生成 API / #1788 AI 生成工作台 / #2045 方案配图自动生成 / #1794 导航入口

#### 2.2.2 图生图 Workbench（tab-i2i.html）

- **组件**: 左上传+控制 / 中并排对比+中线滑块+4 候选 / 右历史输入图
- **数据源**: `POST /api/designai/image/i2i/generate`
- **交互**: 上传原图→调变化强度+LoRA→生成→对比/复用
- **代码现状**: 🔴 新建
- **关联 Issue**: #2313 图生图独立 GUI / #2166 AI 图生图 API

#### 2.2.3 Inpainting 局部修改（tab-inpaint.html）

- **组件**: 工具栏（画笔+橡皮+套索+矩形）/ 大画布+蒙版覆盖 / 4 结果+迭代历史
- **数据源**: `POST /api/designai/image/inpaint`
- **交互**: 圈选区域→描述改动→重绘→迭代满意/重生
- **代码现状**: 🔴 新建
- **关联 Issue**: #2307 局部修改 GUI

#### 2.2.4 素材库联动（tab-assets.html）

- **组件**: 状态徽章 / 入库配置卡（toggle+AI 标签+关联项目+商机+水印）/ 标签词云 / 最近入库 6 张网格
- **数据源**: `POST /api/designai/image/to-asset-library` / `GET /api/asset-library/list?source=designai`
- **交互**: 自动入库 toggle / AI 标签（Qwen3.5-VL）/ 关联到项目和商机
- **代码现状**: 🟡 素材库（biz:asset-library）已有 Controller，本 Tab 只做 bridge
- **关联 Issue**: #1939 素材库集成

#### 2.2.5 生成历史（tab-history.html）

- **组件**: 类型 Tab + 筛选 + 12 行表格 + 分页
- **数据源**: `GET /api/designai/image/history`
- **交互**: 复用 prompt / 送 Inpaint / 送素材库
- **代码现状**: 🔴 新建

**§2.2 三级边界**:
- ✅ Always: 生成任务必须异步+WebSocket 推进度 / LoRA 必须从 Phase3 仓库加载
- ⚠️ Ask First: 新增 Base 模型（影响成本）/ ControlNet 新模式
- 🚫 Never: 不硬编码 API Key / 不跳过费用记录

---

### 2.3 Phase 2 · 视频生成工坊（4 Tab）

**路由**: /wande/design-ai/video
**对标**: Runway Gen-3 / Luma Dream Machine / Kling

#### 2.3.1 AI 图生视频（tab-i2v.html）

- **组件**: 左 参数（摄像机运动 6 选+时长+画质+Motion+模型）/ 中 1024×576 预览+控制条+3 备选 / 右 历史+拼接投标视频
- **数据源**: `POST /api/designai/video/i2v` / `GET /api/designai/video/task/{id}`
- **代码现状**: 🔴 新建；外部依赖 Runway/Luma/Kling API
- **关联 Issue**: #1635 AI 图生视频

#### 2.3.2 Three.js 漫游录制（tab-roaming.html）

- **组件**: K0-K4 关键帧 / 800×500 3D 预览 / 导出选项（4K+格式+音乐+水印+质量）
- **数据源**: `POST /api/designai/video/roaming/render` / WebSocket 进度
- **代码现状**: 🔴 新建；前端依赖 Three.js + mediarecorder
- **关联 Issue**: #2305 Three.js 漫游录制

#### 2.3.3 AI 环境自动填充（tab-environment.html）

- **组件**: 8 类元素多选 / 风格+季节+时段+参考图 / 左右对比 / 局部 refine
- **数据源**: `POST /api/designai/video/environment/fill`
- **代码现状**: 🔴 新建
- **关联 Issue**: #1834 AI 环境自动填充

#### 2.3.4 环境模板库（tab-templates.html）

- **组件**: 10 分类 tab / 12 模板卡 4×3 / AI 共创
- **数据源**: `GET /api/designai/video/templates` / `POST /api/designai/video/templates/apply`
- **代码现状**: 🔴 新建
- **关联 Issue**: #1835 环境模板库

**§2.3 三级边界**:
- ✅ Always: 视频 ≥5s 必须后台异步+企微通知 / 费用预估必须显示
- ⚠️ Ask First: 更换外部视频 API 提供方
- 🚫 Never: 不在前端硬编码 API Key / 不直接调外部 API（走后端代理）

---

### 2.4 Phase 3 · LoRA 训练工坊（4 Tab）🟢 **本 Phase 80% 后端已建**

**路由**: /wande/design-ai/lora
**对标**: ComfyUI + Kohya-ss + Civitai

#### 2.4.1 Caption 自动标注（tab-caption.html）

- **组件**: 左 数据集（3 个）/ 中 图片 600×400 + AI & 人工双栏 Caption / 右 批量控制（Qwen3.5-VL）+ Tag 云
- **数据源**:
  - `POST /api/designai/lora/caption/batch` — 批量启动
  - `POST /api/designai/lora/caption/{id}/edit` — 人工修订
  - `GET /api/designai/lora/dataset/{id}` — 数据集详情
- **代码现状**: 🟢 `pipeline/pipelines/comfyui_rendering/wdpp_lora_data_preprocessor.py`（14KB）已实现
- **关联 Issue**: #2456 Caption 自动标注

#### 2.4.2 LoRA 训练台（tab-training.html）

- **组件**: 左 配置（Base+数据集+触发词+7 超参数+GPU 选择+3 预设）/ 中 Loss+LR 曲线+日志 / 右 Epoch 预览+任务队列
- **数据源**:
  - `POST /api/designai/lora/training/start`
  - `GET /api/designai/lora/training/{id}/metrics` — WebSocket 推送 loss 曲线
  - `POST /api/designai/lora/training/{id}/pause` / `stop`
- **代码现状**: 🟢 `wdpp_lora_trainer.py`（17KB）+ `wdpp_lora_validator.py`（11KB）+ `wdpp_lora_pipeline.sh` 已实现
- **关联 Issue**: #2486 LoRA-A 万德产品风格 / #2487 LoRA-B 国际渲染美学 / #1680 风格 LoRA

#### 2.4.3 模型仓库（tab-models.html）

- **组件**: 4 维筛选 / Civitai 风格 3×3 模型卡网格 / 操作（加载到 Phase1/详情/克隆训练）
- **数据源**:
  - `GET /api/designai/lora/models` — 列表
  - `POST /api/designai/lora/models/{id}/publish` — 上架
  - `POST /api/designai/lora/models/{id}/clone-training` — 克隆训练
- **代码现状**: 🟢 `workflow_config.py`（18KB）已配置 LoRA 模型注册
- **关联 Issue**: （#1680 中含模型库部分需求）

#### 2.4.4 批量渲染引擎（tab-render.html）

- **组件**: 左 任务配置（笛卡尔积 5×3×6 = 90 张）/ 中 进度看板+8 缩略图 / 右 产出库分组树+批量操作
- **数据源**:
  - `POST /api/designai/lora/batch-render/start` — 启动批量渲染
  - `GET /api/designai/lora/batch-render/{id}/progress`
- **代码现状**: 🟢 `render_task_service.py`（13KB）+ `comfyui_api.py` + `comfyui_cli.py` 已完备
- **关联 Issue**: #2489 批量渲染引擎

**§2.4 三级边界**:
- ✅ Always: LoRA 训练必须使用 G7e GPU（不在 m7i 训练）/ 模型 ≥1GB 必须 S3 持久化 / 训练 loss 必须入库
- ⚠️ Ask First: 新增 Base 模型架构（SDXL→Flux）/ 训练超参数大改
- 🚫 Never: 不在 Controller 层直接跑训练（必须走 Pipeline 异步任务）

---

### 2.5 Phase 4 · 方案文本工坊（6 Tab）

**路由**: /wande/design-ai/text
**对标**: Writer + Copy.ai + Jasper

#### 2.5.1 意图路由 Workbench（tab-router.html）

- **组件**: 大输入框 800×120 / 意图识别结果卡（4 要素）/ 4 路由按钮 / 历史记录
- **数据源**: `POST /api/designai/router/parse` — 意图解析（LLM）/ `GET /api/designai/router/history`
- **交互**: 输入→解析→显示意图类型+项目+产品+主题+推荐路由→点击路由按钮跳 Phase
- **代码现状**: 🔴 新建；可复用 `backend/.../wande-ai/.../service/*` 中 LLM 调用基础
- **关联 Issue**: #1849 意图路由 API

#### 2.5.2 方案文本编辑器（tab-proposal.html）

- **组件**: 左 220 章节目录（8 Section + 完成度徽章）/ 中 富文本编辑区 / 右 280 AI 助手
- **数据源**: `POST /api/designai/proposal/draft` / `POST /api/designai/proposal/assist` / `GET /api/designai/proposal/references`
- **交互**: 章节切换 / AI 续写+优化+翻译 / 插入配图（来自 Phase1 历史）/ 参考案例
- **代码现状**: 🔴 新建
- **关联 Issue**: #2310 方案文本模块

#### 2.5.3 招标规范文本生成（tab-tender.html）

- **组件**: 三标融合输入（PDF+产品参数+企业资质）/ 10 章大纲 / 章节 Tab + 匹配度报告
- **数据源**: `POST /api/designai/tender/generate` / `POST /api/designai/tender/export?format=docx|pdf`
- **交互**: 上传招标→选产品+资质→生成→分章节查看→匹配度 98%→导出
- **代码现状**: 🔴 新建；依赖 biz:bidding 已有 Policy+BidPricing 数据
- **关联 Issue**: #1843 招标规范文本自动生成

#### 2.5.4 主题灵感库（tab-inspiration.html）

- **组件**: 三层筛选 / 20 主题卡 4×5 / AI 主题共创
- **数据源**: `GET /api/designai/inspiration/themes` / `POST /api/designai/inspiration/variant` / `POST /api/designai/inspiration/create`
- **交互**: 筛选→卡片→套用到项目（跳 Phase1 生成变体）/ AI 共创新主题
- **代码现状**: 🔴 新建
- **关联 Issue**: #2308 主题方案灵感库

#### 2.5.5 颜色配置器（tab-color.html）

- **组件**: 左 产品选择+5 色板（RAL 编号）+ AI 推荐 / 中 900×550 3D 预览 / 右 方案库（5 保存方案）
- **数据源**: `GET /api/designai/color/products` / `POST /api/designai/color/preview` / `POST /api/designai/color/save`
- **交互**: 选产品→拖色板→AI 推荐→3D 实时预览→保存方案
- **代码现状**: 🔴 新建；依赖 PTC 产品库 BOM 和 3D 模型
- **关联 Issue**: #2309 颜色配置器

#### 2.5.6 需求引导 GUI（tab-guided.html）

- **组件**: 5 步步骤条 / Q1-Q6 结构化问卷（项目类型+年龄+主题+场地+预算+特殊要求）
- **数据源**: `POST /api/designai/guided/step1` / `POST /api/designai/guided/submit`
- **交互**: 逐步填写→AI 实时解析摘要→下一步
- **代码现状**: 🔴 新建
- **关联 Issue**: #2312 完整方案流程 Step1

**§2.5 三级边界**:
- ✅ Always: 所有文本生成必须记录 Token 消耗 / 导出 Word/PDF 必须保留格式
- ⚠️ Ask First: 更换底层 LLM / 新增章节 Schema
- 🚫 Never: 不直接把招标文件原文存数据库（走 S3）

---

## 3. API 契约汇总

### 3.1 Phase1 图像生成
- `POST /api/designai/image/t2i/generate` | `POST /api/designai/image/i2i/generate` | `POST /api/designai/image/inpaint`
- `GET /api/designai/image/history` | `POST /api/designai/image/to-asset-library`

### 3.2 Phase2 视频生成
- `POST /api/designai/video/i2v` | `POST /api/designai/video/roaming/render` | `POST /api/designai/video/environment/fill`
- `GET /api/designai/video/templates` | `POST /api/designai/video/templates/apply`

### 3.3 Phase3 LoRA 训练
- `POST /api/designai/lora/caption/batch` | `POST /api/designai/lora/training/start` | `POST /api/designai/lora/batch-render/start`
- `GET /api/designai/lora/models` | `POST /api/designai/lora/models/{id}/publish`

### 3.4 Phase4 方案文本
- `POST /api/designai/router/parse` | `POST /api/designai/proposal/draft` | `POST /api/designai/tender/generate`
- `GET /api/designai/inspiration/themes` | `POST /api/designai/color/preview` | `POST /api/designai/guided/submit`

## 4. 数据库设计

**新增表（MySQL Flyway）**:
- `wdpp_designai_task` — 统一任务表（task_id/phase/type/user_id/status/input_json/output_json/cost/gpu_seconds）
- `wdpp_designai_image` — 图像生成记录（含 LoRA 链接+Seed+Prompt）
- `wdpp_designai_video` — 视频生成记录
- `wdpp_designai_lora_model` — LoRA 模型仓库（含 base_model/dataset_id/epoch/触发词/下载量/评分）
- `wdpp_designai_lora_training` — 训练任务（含 loss 曲线 JSON）
- `wdpp_designai_dataset` — 数据集（关联图片 S3 key 列表）
- `wdpp_designai_caption` — Caption 记录（图片 ID+AI 文本+人工文本+标签）
- `wdpp_designai_proposal` — 方案文本（章节 JSON+项目关联）
- `wdpp_designai_tender` — 招标文本（关联招标文件+产品+资质）
- `wdpp_designai_theme` — 主题库
- `wdpp_designai_color_scheme` — 配色方案
- `wdpp_designai_intent_log` — 意图路由日志

**Flyway 脚本**: `db/migration_wande_ai/V20260422_01__designai_base.sql` 等

## 5. 前端组件清单

- `frontend/src/views/wande/design-ai/index.vue` — Hub 总览
- `frontend/src/views/wande/design-ai/image/*` — Phase1 5 Tab
- `frontend/src/views/wande/design-ai/video/*` — Phase2 4 Tab
- `frontend/src/views/wande/design-ai/lora/*` — Phase3 4 Tab
- `frontend/src/views/wande/design-ai/text/*` — Phase4 6 Tab
- 依赖：a-tabs / a-table / a-form / WebSocket / Three.js（roaming）/ mediarecorder

## 6. H5 适配

- Phase1/2/4 部分 Tab 支持 H5（作品浏览+简单生成），复杂工作台仅 PC
- Phase3 LoRA 训练仅 PC（GPU 监控为主）
- 路由：`/h5/design-ai/image` 等

## 7. PageGuide 内容

- **这是什么**: AI 创意生产中枢，一站式做图/视频/LoRA/文本
- **解决什么问题**: 投标效果图耗时 / 依赖外部 API 成本高 / 方案文本重复劳动
- **快速上手**:
  1. Phase4 意图路由输入"做个 XX 项目 XX 主题方案"→自动分发
  2. Phase1/2 用 Phase3 自建 LoRA 生成效果图+视频
  3. 全部产出自动入素材库→导出到投标 PPT

## 8. 产品验收清单

### 8.1 Hub 加载
- [ ] 访问 `/wande/design-ai` → 3s 内渲染
- [ ] 6 KPI 数字非 0（有 mock 或真实数据）
- [ ] 4 Phase 入口按钮可跳转
- [ ] 数据流 SVG 清晰

### 8.2 Phase1 图像
- [ ] T2I 生成 30s 内返回 4 张
- [ ] LoRA 下拉从 Phase3 拉取
- [ ] 入库 toggle 切换生效
- [ ] 历史复用 prompt 成功

### 8.3 Phase2 视频
- [ ] I2V 异步生成（任务入库+WebSocket 推进度）
- [ ] Three.js 漫游关键帧可编辑
- [ ] 环境填充对比滑块生效

### 8.4 Phase3 LoRA（重点）
- [ ] Caption 批量 342 张 ETA 可见
- [ ] 训练启动→loss 曲线实时推送
- [ ] 模型仓库"加载到 Phase1"链路打通
- [ ] 批量渲染 90 张笛卡尔积正确

### 8.5 Phase4 文本
- [ ] 意图路由准确率 >90%（20 条测试集）
- [ ] 方案编辑器 8 Section 完成度徽章
- [ ] 招标文本章节匹配度 ≥85%
- [ ] 配色器 3D 实时更新

---

## 版本日志

- v1.0 (2026-04-22): Hub + 4 Phase 全景架构 — 基于 23 个历史 Issue 三重引用回填。Phase3 80% 后端已建可优先上线。
